# Core analysis (Python >= 3.10)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
xgboost>=1.7
shap>=0.44
matplotlib>=3.7
seaborn>=0.13

# Optional
adjustText>=1.0      # non-overlapping labels in volcano/stability plots
requests>=2.31       # STRING enrichment (scripts/05 --string)
pytest>=7.0          # tests/
# gseapy>=1.1        # extras/ only (Enrichr / GSEA)
# networkx>=3.0      # extras/ only (network plots)
